# =============================================================================
# CI PIPELINE - Desabilitado temporariamente
# =============================================================================
# Este workflow está desabilitado para permitir deploys sem testes

name: CI

on:
  workflow_dispatch:  # Apenas manual

jobs:
  skip:
    name: CI Skipped
    runs-on: ubuntu-latest
    steps:
      - name: Skip CI
        run: echo "CI temporariamente desabilitado"
