# =============================================================================
# DEPLOY PRODUCTION - Desabilitado temporariamente
# =============================================================================

name: Deploy to Production

on:
  workflow_dispatch:  # Apenas manual

jobs:
  skip:
    name: Deploy Skipped
    runs-on: ubuntu-latest
    steps:
      - name: Skip Deploy
        run: echo "Deploy production temporariamente desabilitado - use azure-backend.yml"
