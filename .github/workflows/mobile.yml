# =============================================================================
# MOBILE - Desabilitado temporariamente
# =============================================================================

name: Mobile

on:
  workflow_dispatch:  # Apenas manual

jobs:
  skip:
    name: Mobile Skipped
    runs-on: ubuntu-latest
    steps:
      - name: Skip Mobile
        run: echo "Mobile CI temporariamente desabilitado"
